# If you want to random port then we have to give prot value is 0
#server.port=0

#server.port=8081
#server.servlet.context-path=/easyschool

#============================
#CONDITIONS EVALUATION REPORT
#============================
#debug=true

#spring.thymeleaf.cache=false

#-----Spring Security-------------
# ( By default spring security provide the login page to secure the all
# url which is prsent in project so defult username is 'user'
# and password is auto generated on console when run the project )

# If we want to provide the custom username and password then mention in properties file like below.

#spring.security.user.name=karan
#spring.security.user.password=123


#_____________________________________---------H2 Database Configuration----------_________________________________

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#spring.datasource.generate-unique-name=false